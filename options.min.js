function refreshWorkspace() { let e = Object.keys(localStorage), t = document.getElementById("refresher"); if (t.innerHTML = "", e.length) e.map(e => { let n = document.createElement("p"); n.innerText = e; let r = document.createElement("button"); r.setAttribute("id", e), r.setAttribute("class", "getter"); let l = document.createElement("img"); l.src = "images/delete.png", r.appendChild(l); let d = document.createElement("li"); d.appendChild(n), d.appendChild(r), t.appendChild(d) }); else { let e = document.createElement("div"); e.innerText = "[ Empty ]", e.setAttribute("id", "emptyWorkspace"), t.appendChild(e) } deleteButtonHandler() } function deleteButtonHandler() { Array.from(document.getElementsByClassName("getter")).forEach(e => { e.addEventListener("click", () => { localStorage.removeItem(e.id), refreshWorkspace() }) }) } refreshWorkspace(), deleteButtonHandler();